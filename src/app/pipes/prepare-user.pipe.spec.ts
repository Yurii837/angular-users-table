import { PrepareUserPipe } from './prepare-user.pipe';

describe('PrepareUserPipe', () => {
  it('create an instance', () => {
    const pipe = new PrepareUserPipe();
    expect(pipe).toBeTruthy();
  });
});
